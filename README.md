# Hex-grid Creator

A small react component(site) that generates a hex-grid with the help of konva.
You can then export the grid as png or svg.

It's a little quirky but works.

It's 100% AI made so I have no idea what the code looks like. I have changed some texts by hand but thats it.
